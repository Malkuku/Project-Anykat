<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <!-- 输出 ERROR 级别日志到 error.log -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} [%thread] %level %logger{36} - %msg%n</pattern>
        </encoder>
        <!-- 只记录 ERROR 级别 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>  <!-- 匹配 ERROR 则记录 -->
            <onMismatch>DENY</onMismatch>  <!-- 其他级别不记录 -->
        </filter>
    </appender>

    <!-- 输出 INFO 和 WARN 级别日志到 info-warn.log -->
    <appender name="INFO_WARN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/info-warn.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/info-warn.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} [%thread] %level %logger{36} - %msg%n</pattern>
        </encoder>
        <!-- 只记录 INFO 和 WARN 级别 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>  <!-- 匹配 INFO 则记录 -->
            <onMismatch>NEUTRAL</onMismatch>  <!-- 不匹配则继续下一级过滤 -->
        </filter>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <onMatch>ACCEPT</onMatch>  <!-- 匹配 WARN 则记录 -->
            <onMismatch>DENY</onMismatch>  <!-- 其他级别不记录 -->
        </filter>
    </appender>

    <!-- 日志级别设置 -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ERROR_FILE"/>  <!-- 单独记录 ERROR -->
        <appender-ref ref="INFO_WARN_FILE"/>  <!-- 单独记录 INFO 和 WARN -->
    </root>
</configuration>